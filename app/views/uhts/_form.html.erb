<%= form_with(model: uht, local: true) do |form| %>
  <% if uht.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(uht.errors.count, "error") %> prohibited this uht from being saved:</h2>

      <ul>
      <% uht.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.text_field :team_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
